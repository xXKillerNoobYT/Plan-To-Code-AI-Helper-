# You have created unit test code with <<<TestFramework>>> test framework. The code was verified by eslint, but failed.
    # Correct the test code based on below eslint error data.
    # MUST RETURN THE TEST CODE IN THE FORMAT OF ```<<<Language>>>
<Test code>```.

    # The unit test code is:
    ```ts
    <<<Spec>>>
    ```
    # eslint error data:
    ```
    <<<LintErrors>>>
    ```